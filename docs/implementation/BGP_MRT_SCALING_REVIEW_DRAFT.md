# Complementary BGP/MRT hardening review draft

Prepared against `3bc7773cea6dd98a9e8a2ca8a377d301014ae73e`. Worker native Rust validation was not available; this note does not certify test or release completion.

The implementation preserves the upstream general batch reducer, binary peer lookup, compact v2 candidate references, and v1 sealed source-store representation. It adds a mixed-batch final-receipt correction, checked compact-reference joins, tighter source-allocation admission, segmented source-store writes, bounded v2 archive/query projections, root JSON reservation helpers, and exact/verified BGP4MP source-range APIs.

This older review draft predates the current local BGP4MP replay slice. The range APIs are now consumed by source-ordered event and capability-gated UPDATE-candidate replay, which passes the current Windows native gates; the exact published checkpoint `ed4350e` also passes native-validation on Windows/Linux/macOS and the streaming-evidence-contracts workflow, as recorded in [BGP_COMPLETION_HANDOFF.md](BGP_COMPLETION_HANDOFF.md). This is not session Adj-RIB-In or BMP support and does not prove corpus behavior or real scale. Encoding metadata and observed negotiation must remain distinct. Source ranges and hashes do not establish collector authenticity, captured packet evidence, installed routes, reachability, causality, or attack attribution.

The new tests must pass in the real repository before integration is accepted. Require byte-compatible current-v2 output/v1 stores, strict reference tamper rejection including duplicate indices, final mixed-batch conflict receipts, unchanged exact replay behavior, fail-closed limits, no-overwrite publication, and all existing capture/import/state tests. Run native debug/release/no-default-feature, formatting, Clippy, docs, vectors, and exact-commit platform gates. Logical budget counters are not RSS or throughput measurements.

The current source-ordered replay slice and its Windows-local integration gates have since passed, as recorded in `CURRENT.md` and `BGP_COMPLETION_HANDOFF.md`. Remaining immediate work: complete imported session Adj-RIB-In/reset/teardown and captured-versus-imported semantic equivalence; then persisted policy/correlation and bounded BMP; then broader corpus/fuzz/profile/index/recovery qualification. No broader protocol completeness claim is made here.
